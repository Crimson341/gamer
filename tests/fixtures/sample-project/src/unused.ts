/**
 * @deprecated Use the new helper instead.
 */
export function deprecatedHelper(): void {
  // This function is exported but never imported by any other module.
}

export const LEGACY_FLAG = true;
